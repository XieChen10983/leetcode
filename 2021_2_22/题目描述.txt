1. 给你两个版本号 version1 和 version2 ，请你比较它们

2. 给定一个字符串，逐个翻转字符串中的每个单词。无空格字符构成一个单词，输入字符串或者后面包含多余空格，但是反转后不允许。两个单词之间只能有一个空格
    解法：1.可以先取出前后的空格。从前到后vector存入单词，再翻转
         2. 可以先使用reverse方法先全部翻转。然后双指针填充单词，再根据单词的长度用reverse方法翻转单词。

3. 给你一个整数数组 nums ，请你找出数组中乘积最大的连续子数组（该子数组中至少包含一个数字），并返回该子数组所对应的乘积。