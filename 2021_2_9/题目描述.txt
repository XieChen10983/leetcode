1. 给你两棵二叉树的根节点 p 和 q ，编写一个函数来检验这两棵树是否相同
    解法：递归，减而治之。判断是否相等，判断左右是否相等。

2. 给定一个二叉树，检查它是否是镜像对称的。
    解法：递归减而治之，判断两棵树是否对称。得到此函数之后，只需判断树的左右子树是否为镜像对称即可。

3. 给定一个二叉树，找出其最大深度。
    解法：递归减而治之，深度等于1+左右子树的最大深度的更大者。

4. 给定一个二叉树，返回其节点值自底向上的层序遍历。 （即按从叶子节点所在层到根节点所在的层，逐层从左向右遍历）
    解法：使用队列完成层次遍历。接着用vector的翻转。