1. 给定一个没有重复数字的序列，返回其所有可能的全排列
    解法：回溯算法。不用重新构建向量，只需要使用swap函数不断交换，回溯的时候交换回来即可

2. 请编写一个函数，使其可以删除某个链表中给定的非末尾节点，传入函数的唯一参数为要被删除的节点的地址
    因为不知道前面的节点，因此需要将当前节点和下一个节点交换，然后跳过下一个节点即可。

3. 给定一个可以包含重复数字的序列nums，按任意顺序返回所有不重复的全排列。
    解法：和1的一样可以用回溯法，对于不重复的要求，可以添加一个排序，然后加一个当前要加的数是否和前一个数相等的判断从而保证不重复。